---
title: Java接口与抽象类
category: Back-end
tags: [Java, 接口, 抽象类]
date: 2019-12-12 14:23:20
---

### Java接口与抽象类

#### 1. 对接口与抽象类的理解

##### 接口
> 接口是对行为的抽象，可以包含属性和方法；
> + 属性被隐式指定为 public static final，即全局常量；
> + 方法被隐式指定为 public abstract，即抽象方法；
> 

也就是说，接口中所有的方法都必须为抽象方法，不能有具体实现。所以说，接口是对行为的抽象。

##### 抽象类
> 抽象类是对（一类事物）的抽象；
> 定义：包含抽象方法的类；即包含抽象方法的类一定是抽象类；
> 

> 抽象类可以含有属性、方法和构造器
> + 方法可以是普通方法也可以是抽象方法，若为抽象方法则必须为public或protected的，缺省情况下默认为public的，因为抽象方法需要被子类继承和实现。
> + 构造器虽然有，但因为抽象类含有无具体实现的方法，所以抽象类不能进行实例化。
> 

#### 2. 接口与抽象类的区别
![接口与抽象类的区别.png](/images/back-end/java/接口与抽象类的区别.png)

#### 例：门都有打开和关闭两个行为，此时若需要门具备警报行为，应该如何实现呢？

+ 门的打开和关闭属于门本身固有的行为，而警报功能属于门非固有的行为（附加行为）；
+ 将门设计为一个抽象类，包括打开和关闭两种行为；
+ 将警报设计为一个接口，包括警报行为；
+ 设计一个警报门继承抽象类并实现警报接口即可。


